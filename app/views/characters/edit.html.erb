<h1>Edit <%= @character.name %></h1>

<%= render 'form' %>

<%= link_to "Destroy Character", [@character.novel, @character], method: :delete, data: { confirm: 'Are you sure?' } %>

<p>
<%= link_to "Back", novel_path(@novel.id) %>