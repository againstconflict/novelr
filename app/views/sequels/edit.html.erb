<h1>Edit <%= @sequel.name %></h1>

<%= render 'form' %>

<%= link_to "Destroy", [@sequel.novel, @sequel], method: :delete, data: { confirm: 'Are you sure?' } %>

<p>
<%= link_to "Back", novel_path(@novel.id) %>